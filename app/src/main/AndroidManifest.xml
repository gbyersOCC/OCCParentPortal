<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="edu.orangecoastcollege.cs273.gabyers.occparentportal">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ParentMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".TeacherMainActivity"/>
        <activity android:name=".StreamClassVideo"/>
        <activity android:name=".ChooseStudentActivity"/>
        <activity android:name=".RSSActivity"/>
        <activity android:name=".AddPerformanceRatings"/>
        <activity android:name=".NewsDetailsActivity"/>
        <activity android:name=".YouTubeActivity"/>
        <activity android:name=".EditProfileActivity"/>

    </application>

</manifest>